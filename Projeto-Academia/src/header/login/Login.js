import { Link } from "react-router-dom";

export default function () {
    return (
        <Link className="fw-bolder button" to="/login">
            <div>
                LOGIN
            </div>
        </Link>
    )
}